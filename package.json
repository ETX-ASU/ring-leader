{
  "name": "@asu-etx/ring-leader",
  "version": "1.0.0",
  "description": "©, 2020 Arizona State University ETX. Libraries to support LTI 1.3 enabled tools",
  "main": "NONE",
  "repository": "https://github.com/ETX-ASU/ring-leader.git",
  "author": "Melanie.Narish@asu.edu",
  "license": "NOLICENSE",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/rl-shared",
      "packages/rl-server-lib",
      "packages/rl-client-lib",
      "packages/rl-tool-example"
    ]
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^5.2.0",
    "prettier": "2.0.5"
  },
  "engines": {
    "node": ">=8.3.0"
  },
  "scripts": {
    "start": "yarn install && cd packages/rl-shared && yarn run build && cd ../rl-client-lib && yarn run build && cd ../rl-server-lib && yarn run build && cd ../rl-tool-example-client && yarn run build",
    "watch-client-lib": "cd packages/rl-client-lib && yarn run watch",
    "watch-server-lib": "cd packages/rl-browser-server-lib && yarn run watch",
    "watch-shared": "cd packages/rl-shared && yarn run watch",
    "develop": "concurrently --kill-others \"yarn run watch-client-lib\" \"yarn run watch-server-lib\" \"yarn run watch-shared\""
  }
}
